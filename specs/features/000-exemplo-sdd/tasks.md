# Tasks — 000 Exemplo SDD

- [ ] F000-T1 — Entregar resposta 200 com `status` e `timestamp` via RED → GREEN → REFACTOR (F000-R1, F000-R2)
- [ ] F000-T2 — Registrar rota `GET /health` via RED → GREEN → REFACTOR (F000-R1)
- [ ] F000-T3 — Entregar resposta 503 via RED → GREEN → REFACTOR (F000-R3)

> Tasks desmarcadas de propósito — este é um modelo, não uma implementação.
