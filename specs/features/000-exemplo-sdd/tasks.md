# Tasks — 000 Exemplo SDD

- [ ] T1 — Criar `src/health.ts` com handler que retorna 200 + JSON (R1, R2)
- [ ] T2 — Registrar rota `GET /health` em `src/index.ts` (R1)
- [ ] T3 — Implementar branch 503 quando serviço indisponível (R3)
- [ ] T4 — Teste: resposta 200 com `status` e `timestamp` (R1, R2)
- [ ] T5 — Teste: resposta 503 quando indisponível (R3)

> Tasks desmarcadas de propósito — este é um modelo, não uma implementação.
