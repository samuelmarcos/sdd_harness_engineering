# BACKLOG — Features do projeto

> Lista priorizada de features. Status reflete o `status.json` de cada uma.
> Adicione novas linhas no topo da seção "Pendentes".

## Legenda

`pending` → no backlog · `awaiting_approval` → aguardando humano · `approved`
→ liberada · `in_progress` → implementando · `in_review` → revisando ·
`verified` → validada · `done` → concluída

---

## Pendentes (pending)

_(nenhuma — adicione sua próxima feature aqui)_

<!-- Exemplo:
| ID | Feature | Prioridade |
|----|---------|------------|
| 001 | Autenticação JWT | alta |
-->

---

## Em andamento / especificadas

_(nenhuma)_

---

## Referência (fora do backlog de produção)

| ID | Feature | Estado |
|----|---------|--------|
| 000 | Exemplo SDD (referência de formato) | `pending` |

> A feature `000-exemplo-sdd` é um modelo, não uma feature concluída.

---

## Como adicionar uma feature

1. Organize o backlog: **`/roadmap`** (recomendado) ou linha manual em **Pendentes**.
2. Opcional: **`/integracoes`** se specs precisarem de dados reais de ferramentas.
3. No Claude Code: **"Nova feature: \<descrição\>"** (skill `sdd-init`).
4. Brownfield: confirme **`## Contexto as-is`** no `design.md` (pós-`/mapear`).
5. Decisão ambígua: **`/clarificar`** antes de aprovar a spec.
